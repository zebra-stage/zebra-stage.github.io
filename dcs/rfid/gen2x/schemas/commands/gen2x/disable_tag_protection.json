{
  "title": "disable_tag_protection",
  "x-summary": "Disable Protected Mode",
  "description": "Removes the TagProtect lock from a specific tag. Once disabled, the tag becomes visible to all readers again without needing any password.",
  "x-tag": "Protected Mode",
  "type": "object",
  "required": ["command", "command_id", "payload"],
  "properties": {
    "command": {
      "type": "string",
      "const": "set_impinjGen2X",
      "description": "MQTT command name"
    },
    "command_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier to correlate request with response"
    },
    "payload": {
      "type": "object",
      "required": ["tagProtect"],
      "properties": {
        "tagProtect": {
          "type": "object",
          "required": ["action", "password", "tagID"],
          "properties": {
            "action": {
              "type": "string",
              "const": "disableTagProtection",
              "description": "Disables Protected Mode on the tag"
            },
            "password": {
              "type": "string",
              "minLength": 8,
              "maxLength": 8,
              "pattern": "^[0-9A-Fa-f]{8}$",
              "description": "8-character hexadecimal 32-bit password"
            },
            "tagID": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]+$",
              "description": "Tag EPC ID"
            }
          },
          "description": "Protected Mode configuration"
        }
      }
    }
  },
  "examples": [
    {
      "command": "set_impinjGen2X",
      "command_id": "a157818d-9667-4d9f-8181-114ab48306f6",
      "payload": {
        "tagProtect": {
          "action": "disableTagProtection",
          "password": "77777777",
          "tagID": "12348765a5050076c4d75211"
        }
      }
    }
  ]
}