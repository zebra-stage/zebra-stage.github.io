{
    "title": "disable_fastid",
    "x-summary": "Disable FastID",
    "description": "Disables FastID. Tags return only EPC per scan (normal mode).",
    "x-tag": "FastID",
    "type": "object",
    "required": [
        "command",
        "command_id",
        "payload"
    ],
    "properties": {
        "command": {
            "type": "string",
            "const": "set_impinjGen2X",
            "description": "MQTT command name"
        },
        "command_id": {
            "type": "string",
            "format": "uuid",
            "description": "Unique identifier to correlate request with response"
        },
        "payload": {
            "type": "object",
            "required": [
                "fastID"
            ],
            "properties": {
                "fastID": {
                    "type": "object",
                    "required": [
                        "enabled"
                    ],
                    "properties": {
                        "enabled": {
                            "type": "boolean",
                            "const": false,
                            "enum": [false],
                            "description": "Disable FastID feature"
                        }
                    },
                    "description": "FastID feature configuration"
                }
            },
            "description": "Command payload containing the feature configuration"
        }
    },
    "examples": [
        {
            "command": "set_impinjGen2X",
            "command_id": "b35808c8-0e10-42a1-8f81-799e7843e5c9",
            "payload": {
                "fastID": {
                    "enabled": false
                }
            }
        }
    ]
}
