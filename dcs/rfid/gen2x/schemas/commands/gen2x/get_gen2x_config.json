{
    "title": "get_gen2x_config",
    "description": "Retrieves the last saved Gen2X feature from the reader. After any successful PUT, exactly one feature object is returned. If Gen2X has never been configured, MQTT payload is {} and REST GET returns an empty body.",
    "x-tag": "Gen2X Configuration Retrieval",
    "type": "object",
    "required": [
        "command",
        "command_id",
        "payload"
    ],
    "properties": {
        "command": {
            "type": "string",
            "const": "get_impinjGen2X",
            "description": "MQTT command name"
        },
        "command_id": {
            "type": "string",
            "format": "uuid",
            "description": "Unique identifier to correlate request with response"
        },
        "payload": {
            "type": "object",
            "description": "Empty payload for get commands"
        }
    },
    "examples": [
        {
            "command": "get_impinjGen2X",
            "command_id": "9d744286-7b97-40b8-9e91-190df0334557",
            "payload": {}
        }
    ]
}
