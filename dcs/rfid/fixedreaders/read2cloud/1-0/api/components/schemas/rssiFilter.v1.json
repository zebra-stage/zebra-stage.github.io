{
  "title": "rssiFilter",
  "type": "object",
  "description": "Tag with RSSI below threshold will not be inventoried by the radio\nNote: Currently ONLY supported by the FX9600",
  "x-examples": {},
  "properties": {
    "threshold": {
      "type": "integer",
      "description": "Threshold (in dbm) below which the radio will ignore a tag response",
      "format": "int32",
      "minimum": -128,
      "maximum": -20
    }
  }
}